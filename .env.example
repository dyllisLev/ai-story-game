# Server Configuration
NODE_ENV=development
PORT=5000

# Session Secret (REQUIRED - Change this in production!)
# Generate with: openssl rand -base64 32
SESSION_SECRET=change-this-to-a-random-secret-key-at-least-32-characters

# Database (Optional - Using SQLite by default)
# Uncomment to use PostgreSQL instead
# DATABASE_URL=postgresql://username:password@localhost:5432/storycracker

# AI API Keys (Optional - Users can set in account settings)
# These are fallback keys for unauthenticated requests
# API_KEY_CHATGPT=
# API_KEY_GEMINI=
# API_KEY_CLAUDE=
# API_KEY_GROK=

# Docker Secrets (For GitHub Actions)
# DOCKER_USERNAME=
# DOCKER_PASSWORD=
